# TODO

Все пункты из прошлого списка выполнены:

- [x] API key персистится в `provider.json` (права 600), форма настроек показывает сохранённый ключ с иконкой глаза для показа/скрытия.
- [x] Автоскролл чата к последнему сообщению (с учётом ручного скролла пользователя вверх), инпут зафиксирован отдельной строкой grid, не уезжает с чатом.
- [x] Markdown-рендеринг финального ответа ассистента (заголовки, списки, код, ссылки, bold/italic).
- [x] Tool calls в одну строку с цветным тегом и стрелкой-fold: `[bash]` зелёный, `[read]` синий, `[write]` фиолетовый, `[edit]` янтарный, `[grep/find/ls]` голубой.
- [x] Vercel-style тёмная нейтральная палитра с акцентами (`src/styles.css`, CSS-переменные `--accent-*`).
